{
    "datacenter": "you-tag",
    "data_dir": "{{ path_consul }}/data",
    "log_level": "INFO",
    "enable_syslog": true,
    "disable_update_check": true,
    "node_name" : "{{ ansible_hostname }}",
    "bind_addr": "{{ ip_srv_consul.stdout }}",
    "advertise_addr": "{{ ip_srv_consul.stdout }}",
    "client_addr": "{{ ip_srv_consul.stdout }}",
    "ui": true,
    "server" : true,
    "bootstrap_expect": 2,
    "start_join": [{% for host in srv_hostname_consul %} "{{ host.name }}" {{ host.demiliter }} {% endfor %}]
}
